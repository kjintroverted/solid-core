version: 2
jobs:
  build:
    docker:
      - image: cimg/node:15.5.1-browsers
    working_directory: ~/solid-core
    steps:
      - checkout
      - run: npm i
      # Add Tests
      - run: npm run build

  deploy:
    docker:
      - image: cimg/node:15.5.1-browsers
    working_directory: ~/solid-kitchen
    steps:
      - checkout
      - add_ssh_keys:
          fingerprints:
            - "b8:78:e9:fe:e2:dc:58:45:d2:36:b5:5e:a1:4a:be:c0"
      - run: npm i
      - run: npm run build
      - run: git checkout -b release
      - run: npm version patch
      - run: git config user.email "wkgreen13@gmail.com"
      - run: git config user.name "CircleCI"
      - run: git commit -m "CI Deploy"
      - run: git push -u origin release 

workflows:
  version: 2
  build-deploy:
    jobs:
      - deploy:
          filters:
            branches:
              only: master
